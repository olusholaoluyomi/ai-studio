# HuggingFace Spaces — requirements (this is the file HF uses automatically)

# ── Build tools ────────────────────────────────────────────────────────────────
setuptools>=68.0
wheel

# ── Core ──────────────────────────────────────────────────────────────────────
gradio>=5.14.0,<6
numpy<2
pydantic==2.9.1
python-dotenv>=1.0.0
pyyaml>=6.0
rich>=13.0

# ── PyTorch CPU build ─────────────────────────────────────────────────────────
--extra-index-url https://download.pytorch.org/whl/cpu
torch
torchaudio

# ── Speech recognition ────────────────────────────────────────────────────────
# ctranslate2 4.4.0 is the last version that works on CPU without cuDNN v9
# (v4.5+ requires cuDNN v9 which is not present on HF CPU spaces)
ctranslate2==4.4.0
faster-whisper==1.1.0
# av>=12.0.0 has prebuilt manylinux wheels; av<=11 has source-only (needs libav-dev headers)
av>=12.0.0
openai-whisper @ git+https://github.com/openai/whisper.git
whisper-timestamped>=1.15.0
whisperx==3.3.1
ffmpeg-python==0.2.0
librosa==0.10.1
pydub==0.25.1
soundfile
pyworld==0.3.4
noisereduce

# ── TTS ───────────────────────────────────────────────────────────────────────
spacy>=3.7.0
edge-tts
python-iso639
pysubs2
phonemizer
kokoro
misaki[en]
misaki[ja]
misaki[zh]
f5-tts>=1.0.0

# ── Translation & NLP ─────────────────────────────────────────────────────────
deep-translator
lingua-language-detector
azure-ai-translation-text==1.0.0b1
azure-cognitiveservices-speech

# ── Video / image generation ──────────────────────────────────────────────────
diffusers==0.29.0
accelerate>=0.26.0
# transformers 5.x requires torch>=2.4; HF CPU spaces have torch 2.1.x
transformers>=4.40.0,<5.0.0
imageio[ffmpeg]>=2.34.0
imageio-ffmpeg>=0.4.9

# ── Audio generation (GPU-only, installed for import; won't run on CPU) ────────
audiocraft

# ── Utilities ─────────────────────────────────────────────────────────────────
huggingface-hub>=0.27.1
cached_path>=1.6.0
pandas>=2.0.0
matplotlib
tqdm
structlog
yt-dlp>=2024.12.13
more_itertools
json5
inflect>=7.0.0
websockets
websocket-client
jiwer
markdown
py-cpuinfo==9.0.0
wget
uuid
kaldialign
evaluate

# ── CosyVoice dependencies ────────────────────────────────────────────────────
conformer==0.3.2
gdown==5.1.0
grpcio==1.57.0
grpcio-tools==1.57.0
HyperPyYAML==1.2.2
lightning==2.2.4
modelscope==1.15.0
tensorboard==2.14.0
WeTextProcessing==1.0.3

# ── Websocket / live features ─────────────────────────────────────────────────
websockets
websocket-client
