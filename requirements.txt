# HuggingFace Spaces — requirements.txt
# GPU-only packages (audiocraft, modelscope, cosyvoice deps) are EXCLUDED:
# audiocraft pins torch==2.1.x which conflicts with transformers 4.40+
# modelscope/cosyvoice are GPU-only and also conflict with torch versions

# ── Build tools ────────────────────────────────────────────────────────────────
setuptools>=68.0
wheel

# ── Core ──────────────────────────────────────────────────────────────────────
gradio>=5.14.0,<6
numpy<2
pydantic==2.9.1
python-dotenv>=1.0.0
pyyaml>=6.0
rich>=13.0

# ── PyTorch CPU — let pip resolve freely (need >=2.2 for transformers 4.40+) ──
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.2.0
torchaudio

# ── Speech recognition ─────────────────────────────────────────────────────────
# ctranslate2 4.4.0: last version that works on HF CPU (no cuDNN v9 required)
ctranslate2==4.4.0
faster-whisper==1.1.0
av>=12.0.0
openai-whisper @ git+https://github.com/openai/whisper.git
whisper-timestamped>=1.15.0
whisperx==3.3.1
ffmpeg-python==0.2.0
librosa==0.10.1
pydub==0.25.1
soundfile
noisereduce

# ── TTS ────────────────────────────────────────────────────────────────────────
spacy>=3.7.0,<3.9.0
edge-tts
python-iso639
pysubs2
phonemizer
kokoro
misaki[en]
# misaki[ja] and misaki[zh] excluded: pyopenjtalk/jieba need heavy compilation
f5-tts>=1.0.0

# ── Translation & NLP ──────────────────────────────────────────────────────────
deep-translator
lingua-language-detector
azure-ai-translation-text==1.0.0b1
azure-cognitiveservices-speech

# ── Video / image generation ───────────────────────────────────────────────────
# diffusers 0.30+ uses torch.xpu (requires PyTorch 2.4+); pin to 0.29.0
diffusers==0.29.0
accelerate>=0.26.0
# transformers 5.x requires torch>=2.4 and uses APIs not in 2.2.x
transformers>=4.40.0,<5.0.0
imageio[ffmpeg]>=2.34.0
imageio-ffmpeg>=0.4.9

# ── Audio separation ───────────────────────────────────────────────────────────
demucs==4.0.1

# ── Utilities ──────────────────────────────────────────────────────────────────
huggingface-hub>=0.27.1
cached_path>=1.6.0
pandas>=2.0.0
matplotlib
tqdm
structlog
yt-dlp>=2024.12.13
more_itertools
json5
inflect>=7.0.0
websockets
websocket-client
jiwer
markdown
py-cpuinfo==9.0.0
wget
uuid
kaldialign
evaluate
