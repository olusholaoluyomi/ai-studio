# HuggingFace Spaces — requirements
# Lean build: heavy GPU-only packages (f5-tts, cosyvoice, audiocraft, demucs)
# are imported lazily in app.py and fail gracefully on CPU tier.

# ── Build tools ────────────────────────────────────────────────────────────────
setuptools>=68.0
wheel

# ── Core ──────────────────────────────────────────────────────────────────────
numpy<2
pydantic==2.9.1
python-dotenv>=1.0.0
pyyaml>=6.0
rich>=13.0

# ── Speech recognition (faster-whisper replaces openai-whisper) ───────────────
faster-whisper>=1.1.0
ffmpeg-python==0.2.0
librosa==0.10.1
pydub==0.25.1
soundfile
pyworld==0.3.4

# ── TTS ───────────────────────────────────────────────────────────────────────
edge-tts
python-iso639
pysubs2

# ── Translation & NLP ─────────────────────────────────────────────────────────
deep-translator
lingua-language-detector
azure-ai-translation-text==1.0.0b1
azure-cognitiveservices-speech

# ── Video / image generation ──────────────────────────────────────────────────
diffusers>=0.29.0
accelerate>=0.26.0
transformers>=4.40.0
imageio[ffmpeg]>=2.34.0
imageio-ffmpeg>=0.4.9

# ── Utilities ─────────────────────────────────────────────────────────────────
huggingface-hub>=0.27.1
pandas>=2.0.0
matplotlib
tqdm
structlog
yt-dlp
more_itertools
json5
inflect>=7.0.0
websockets
websocket-client
jiwer
