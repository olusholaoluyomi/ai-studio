# requirements-hf.txt — reference / legacy file (not used by HF directly)
# HF Spaces uses requirements.txt at the repo root. This file is kept for
# documentation purposes only. See requirements.txt for the authoritative list.
#
# Key pins that must stay in sync with requirements.txt:
#   ctranslate2==4.4.0      (last CPU-safe version, no cuDNN v9 required)
#   faster-whisper==1.1.0   (compatible with ctranslate2 4.4.0)
#   gradio>=5.14.0,<6
#   yt-dlp>=2024.12.13      (valid released version)
#   pandas>=2.0.0
